<div class="card border-0 mx-auto mb-4" style="width: 30rem;">
  <h3 class="mt-4">Ready to mentor?</h3>
  <!--Buttons to add and remove from mentor list-->
  <button type="submit" class="mx-auto btn blue-button" id="mentorBtn">Add me to the mentor list!</button>
  <button type="submit" class="mx-auto btn maroon-button mb-4" id="removeMentorBtn">Stop Mentoring</button>
 </div>

<!--List of mentors-->
<div class="container">
  <br>
    <h4 class="color-light ml-2">Choose a mentor:</h4>

<!--cards for each mentor-->
  <div class="card-mentor border-0 mb-4" style="width: 20rem;">
    <div class="card-body">
      <h5 class="card-title mb-4">Name Goes Here {{name}}</h5>
      <h6 class="card-subtitle mb-2">About Me:</h6>
      <p class="card-text mb-4">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
      <h6 class="card-subtitle mb-2">My Skills:{{languages}}</h6>
    </div>
  </div>


{{#each mentorProfiles as |profile|}}

        <p>{{about}}</p>

        
      </div>
    </div>
    <div class="col-md-4">
      <button class="btn btn-primary" data-id="{{id}}">Choose this mentor</button>
    </div>
  </div>
{{/each}}
</div>
</div>
</div>

<script src="./js/dashboard.js"></script>




{{!-- this is the older code that i couldn't get to show up...commenting it out for now --}}
  {{!-- <h4 class="color-light ml-2">Choose a mentor:</h4>
  <div class="card border-0 mb-4" style="width: 20rem;">
    example {{#each mentorProfiles as |profile|}}
  
    <div class="row mb-2">
      <div class="col-md-8">
        <h5>name goes here {{name}}</h5>
      </div>
      <div class="col-md-8">
        <p>About Me:</p>
        <p>{{about}}</p>
        <p>My Skills:</p>
        <p>{{languages}}</p>
      </div>
    </div>
    <div class="col-md-4">
      <button class="btn btn-primary" data-id="{{id}}">Choose this mentor</button>
    </div>
  </div>
{{/each}}
</div>
</div>
</div> --}}
